import { ShortSizeDirective } from './short-size.directive';

describe('ShortSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShortSizeDirective();
    expect(directive).toBeTruthy();
  });
});
