import { generateBaseNumbers } from './generate-base-numbers';

describe('GenerateBaseNumbers', () => {
  it('should create an instance', () => {
    expect(generateBaseNumbers()).toBeTruthy();
  });
});
